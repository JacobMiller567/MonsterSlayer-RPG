
/*
if (room != (rMainMenu or rGameover or rShop)) // any other room besides these
{
	
	if (keyboard_check_pressed(ord("P")))
	{
		paused = !paused;
		
		if (paused == false)
		{
			instance_activate_all();
			surface_free(paused_ID);
				paused_ID = -1;
		}
	}
	
	if (paused == true)
	{
		alarm[0]++;
		alarm[1]++;
	}
	
	
}

*/	

